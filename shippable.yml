language: c

compiler:
  - arm-none-eabi-gcc

build:
  ci:
    - chmod +x install.sh
    - ./install.sh
    - apt-file search cc1 | grep 'cc1$'
    - cp /usr/bin/arm-none-eabi-gcc /bin/arm-none-eabi-gcc 
    - make -C src/pca10040/blank/armgcc/
