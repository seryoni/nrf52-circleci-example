language: none

compiler:
  - arm-none-eabi-gcc

build:
  pre_ci_boot:
    image_name: vidavidorra/u14_gnu_arm_embedded
    image_tag: latest
    pull: true
  ci:
    - make -C src/pca10040/blank/armgcc/
